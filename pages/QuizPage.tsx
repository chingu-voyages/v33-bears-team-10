import React from 'react';
// import styled from 'styled-components';

const QuizPage = () => {
  // Take in array of users top songs, return quiz card based off of that.
  // array of favorite songs must have a url

  // will take in the type of game selected.
  return <div>This is the quiz display page!</div>;
};

export default QuizPage;
